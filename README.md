# How to run?

## Running regularly

```bash
node index.js
```

## Running with Sprkl

```bash
sprkl --esm -- node index.js
```

Then, trigger the server by calling the deployed server API, at any path:

```bash
curl localhost:3000
```
